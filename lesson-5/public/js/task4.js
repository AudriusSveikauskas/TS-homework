export const showGenderIncome = ({ sex, income, }) => ({ sex, income: Number(income) });
export const printPersonGenderIncome = ({ sex, income, }) => {
    console.log(`\t${sex}, ${income}`);
};
//# sourceMappingURL=task4.js.map