export const showMaleGender = (arr) => arr
    .filter((person) => person.sex === 'male');
export const printMaleGenderPerson = ({ name, surname }) => {
    console.log(`\t${name} ${surname}`);
};
//# sourceMappingURL=task6.js.map